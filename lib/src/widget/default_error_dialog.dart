import 'package:flutter/material.dart';

class DefaultErrorDialog extends StatelessWidget {
  const DefaultErrorDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Dialog();
  }
}
